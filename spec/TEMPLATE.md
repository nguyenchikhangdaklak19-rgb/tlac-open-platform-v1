# Spec: <Feature name>

> Copy this file to `spec/<feature-name>.md` and fill in every section.
> The `tech-lead` decomposes this into tasks; the `reviewer` verifies against the
> Acceptance Criteria below.

## Goal

<One or two sentences: what are we building and why does it matter? State the
outcome, not the implementation.>

## Users & context

<Who uses this and in what situation? What do they have in front of them, what
are they trying to accomplish, and what do they expect to happen? Include any
constraints (platform, data, performance, deadlines) that shape the work.>

## Acceptance Criteria

> Written as "when X then Y" checks that a **non-technical product owner** can
> verify by **using the product** — no code, logs, or internals. Each line is a
> pass/fail check the reviewer will confirm.

- [ ] When <the user does X>, then <the observable result Y happens>.
- [ ] When <a different situation X>, then <the observable result Y>.
- [ ] When <an error or edge case X, e.g. empty input>, then <the product
      responds with Y rather than failing>.

## Out of scope

<What this feature explicitly does NOT include. List the tempting-but-excluded
items so engineers don't gold-plate and the reviewer doesn't block on things that
were never in scope.>

## Notes

<Open questions, assumptions, links to designs or related specs, and any
decisions already made. Anything that helps the team build the right thing.>
